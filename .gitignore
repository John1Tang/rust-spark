# Rust / Cargo
/target/
**/target/
**/*.rs.bk
Cargo.lock.bak

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Claude / preview
/.claude/

# Build artifacts
*.o
*.so
*.dylib
*.dll
*.exe

# Local env / secrets
.env
.env.local
*.local

# Scratch docker build variants (kept locally during debugging; not
# part of the production image build). The canonical build is
# `docker/Dockerfile`.
/docker/Dockerfile.host-registry
/docker/Dockerfile.linux-build
/docker/Dockerfile.swap
